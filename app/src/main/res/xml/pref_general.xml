<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_value_sort_by"
        android:inputType="textCapWords"
        android:key="@string/pref_key_sort_by"
        android:entries="@array/pref_entries_sort_by"
        android:entryValues="@array/pref_values_sort_by"
        android:title="@string/pref_title_sort_by" />

</PreferenceScreen>
